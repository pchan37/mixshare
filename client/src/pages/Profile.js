import React from 'react';

import { MainPageLayout, UserProfile } from '../components';

const Profile = () => {
  return (
    <MainPageLayout>
      <UserProfile />
    </MainPageLayout>
  );
};

export default Profile;
